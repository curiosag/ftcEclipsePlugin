StyledText.redrawRange(int start, int length, boolean clearBackground) clearBackground ... can be false


IWorkbenchWindow window = HandlerUtil.getActiveWorkbenchWindowChecked(event);
		
ftcClientController
public Completions get(String text, int cursorPos) { ... cursorPos not used
optionale re-evaluation bei gleich bleibender query in syntaxelementsource.get()

progress
schöne completion proposals
nach patch refresh syntyx colors
AsyncWork.goUnderground ln 35 -> CancellationException


comments??
completions with description

bug close editor and reopen file -> failed to create the part's control

Convention stuff

ftcClientController.authenticate() authorize -> authenticate
before model element completions -> authorize
view: del columns
Options
completions image?
    
# plugin.properties
in Manifest.MF of plugin.xml add: Bundle-Localization: plugin
add plugin.properties in build.properties
